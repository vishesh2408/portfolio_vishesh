# Backend

Minimal Express backend for the portfolio monorepo.

To run locally:

```powershell
Set-Location 'C:\Users\hp\Desktop\SEM 6\portfolio\backend'
npm install
npm start
```

API endpoints:
- `GET /api/health` - returns { status: 'ok' }
- `GET /` - basic health/message
